# Railway project configuration
# This monorepo deploys as 3 separate Railway services.
# See docs/deployment.md for full setup instructions.

[build]
builder = "NIXPACKS"
nixPkgs = ["nodejs_22", "python3"]
aptPkgs = ["build-essential", "libudev-dev"]
